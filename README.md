# EarthEcho

Run nmp install to install dependencies

# run in terminal, to auto recompile every change
npm run watch-css

# discuss about best practice for account deletion 
# delete completely from db/set username to NULL/have col called deleted set default to 0
